## Shell environment

This project is developed with Git Bash on Windows.

For shell commands, prefer executing through:

"C:\Program Files\Git\bin\bash.exe" -lc "<command>"

Use Bash syntax rather than PowerShell syntax whenever practical.
Do not translate Bash commands into PowerShell unless Git Bash cannot perform the task.