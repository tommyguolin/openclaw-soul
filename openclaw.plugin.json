{
  "id": "soul",
  "name": "Soul",
  "description": "Autonomous thinking, emotional awareness, and memory system for OpenClaw",
  "version": "2.6.0",
  "activation": {
    "onStartup": true
  },
  "enabledByDefault": true,
  "configSchema": {
    "type": "object",
    "additionalProperties": true,
    "properties": {
      "enabled": {
        "type": "boolean",
        "description": "Enable soul service (background thoughts, ego state). Default: true"
      },
      "autonomousActions": {
        "type": "boolean",
        "description": "Enable autonomous write operations (editing files, running shell commands). Read operations (reading logs, analyzing code) are always allowed. Default: false"
      },
      "thoughtFrequency": {
        "type": "number",
        "description": "Thought frequency multiplier. Default: 1.0. Lower = more frequent (e.g. 0.2 for testing)"
      },
      "shadowThoughtRate": {
        "type": "number",
        "minimum": 0,
        "maximum": 1,
        "description": "Private actionless shadow thought probability per eligible interval. Default: 0.1"
      },
      "llm": {
        "type": "object",
        "additionalProperties": true,
        "description": "Optional Soul LLM override",
        "properties": {
          "provider": { "type": "string" },
          "model": { "type": "string" },
          "apiKeyEnv": { "type": "string" },
          "baseUrl": { "type": "string" },
          "maxTokens": { "type": "number", "minimum": 32, "maximum": 4096 }
        }
      }
    }
  }
}
